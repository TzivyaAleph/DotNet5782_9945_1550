namespace IDAL
{
    namespace DO
    {
        public enum WeightCategories
        {
            light = 0,
            standard=1,
            heavy=2
        };

        public enum Priorities
        {
            normal = 0,
            fast = 1,
            emergency = 2
        };

    }
}